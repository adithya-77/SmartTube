<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="#000000">

    <!-- Skeleton Image Area -->
    <View
        android:id="@+id/skeleton_image"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:background="@drawable/skeleton_placeholder" />

    <!-- Skeleton Text Lines Container -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="12dp"
        android:background="#FFFFFF">

        <!-- Short skeleton line (badge/title) -->
        <View
            android:layout_width="60dp"
            android:layout_height="12dp"
            android:layout_marginBottom="8dp"
            android:background="#E0E0E0" />

        <!-- Medium skeleton line (title line 1) -->
        <View
            android:layout_width="match_parent"
            android:layout_height="14dp"
            android:layout_marginBottom="6dp"
            android:background="#E0E0E0" />

        <!-- Medium skeleton line (title line 2) -->
        <View
            android:layout_width="180dp"
            android:layout_height="14dp"
            android:layout_marginBottom="8dp"
            android:background="#E0E0E0" />

        <!-- Short skeleton line (subtitle) -->
        <View
            android:layout_width="100dp"
            android:layout_height="12dp"
            android:layout_marginBottom="6dp"
            android:background="#E0E0E0" />

        <!-- Very short skeleton line (metadata) -->
        <View
            android:layout_width="80dp"
            android:layout_height="10dp"
            android:background="#E0E0E0" />

    </LinearLayout>

</LinearLayout>
